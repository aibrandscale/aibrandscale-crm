# Shared secret used by the landing site (or any caller) when POSTing to /api/webhooks/lead.
# If unset, the endpoint accepts any request — set this in production.
WEBHOOK_SECRET=change-me-to-a-long-random-string
